import styled from 'styled-components';
import DefaultButton from './DefaultButton';

const HomePageButton = styled(DefaultButton)`
	font-size: 18px;
	font-weight: 700;
	padding: 10px 20px;
`;

export default HomePageButton;
